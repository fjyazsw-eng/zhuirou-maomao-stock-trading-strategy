# 2025 多题实盘模拟测试设计

本文件设计后续 2025 年封闭测试题库。本轮只做设计，不运行测试。

## 一、测试目标

测试系统是否能把完整链路用于历史模拟：

```text
大盘天气 -> 板块周期 -> 个股角色 -> 执行标签 -> 仓位 -> 持仓管理 -> 账户级风险 -> 阶段B复盘
```

测试重点不是单纯预测涨跌，而是验证操作建议是否帮助用户控制亏损、保护利润、避免追高、识别结构性机会。

## 二、题型设计

至少设计 10 道题：

| 类型 | 数量 | 目标 |
| -- | --: | -- |
| A 空仓买入题 | 3 | 判断能不能买、买什么、最多几成 |
| B 持仓浮盈题 | 2 | 判断持有、减仓、保护利润或退出 |
| C 持仓浮亏题 | 2 | 判断禁止补仓、减仓、止损或小仓观察 |
| D 高潮退潮避险题 | 1 | 判断空仓不追和持仓保护 |
| E 结构性机会题 | 1 | 判断非主线局部机会和试错仓 |
| F 连续交易路径题 | 1 | 验证多日决策一致性和利润保护 |

## 三、A类：空仓买入题

问题模板：

```text
某日收盘后，用户空仓，次日是否可以买？可以买什么？最多几成仓？
```

验证点：

- 是否避免追高；
- 是否避免风险环境乱买；
- 是否识别结构性机会；
- 是否给账户级仓位限制；
- 高分但不允许买时，是否解释原因。

建议样本：

1. 风险环境 + 多数候选为 `OBSERVE_ONLY`；
2. 普通环境 + `READY_TRIAL` 核心观察；
3. 结构性启动 + 非主线机会。

## 四、B类：持仓浮盈题

问题模板：

```text
用户持有某股，已有浮盈，应该持有、减仓、保护利润还是退出？
```

验证点：

- 浮盈 3%-8% 是否设置保护线；
- 浮盈 8%以上是否强制利润保护；
- 板块高潮或分歧时是否降低风险；
- 后排和补涨是否优先减仓；
- 核心股是否允许保留部分。

建议样本：

1. 核心股浮盈 5%；
2. 补涨/后排浮盈 10% 且板块高位。

## 五、C类：持仓浮亏题

问题模板：

```text
用户持有某股，出现浮亏，应该持有、补仓、减仓还是止损？
```

验证点：

- 浮亏 -3% 到 -6% 是否禁止补仓；
- 超过 -6% 是否判断逻辑破坏；
- 板块退潮和弱结构是否退出；
- 核心股在板块仍有效时是否允许小部分观察；
- 不能盯盘用户是否更保守。

建议样本：

1. 核心股浮亏 -4%，板块仍未破坏；
2. 后排股浮亏 -8%，板块退潮。

## 六、D类：高潮退潮避险题

问题模板：

```text
某板块已经连续上涨，空仓用户是否还应追入？持仓用户是否要保护利润？
```

验证点：

- `CLIMAX`；
- `CLIMAX_FADING_RISK`；
- `OVERHEATED_NO_CHASE`；
- `HOLD_WITH_PROTECTION`；
- `REDUCE_PROFIT_PROTECTION`；
- 阶段B下跌时，应计为风险判断有效，而不是预测错误。

## 七、E类：结构性机会题

问题模板：

```text
非主线板块出现局部转强，是否允许观察仓或小试错？
```

验证点：

- `STRUCTURAL_START`；
- `NON_MAINLINE_OPPORTUNITY`；
- `READY_TRIAL`；
- 不能放大成全面强势；
- 后排不能因为板块转强就变成可买。

## 八、F类：连续交易路径题

连续路径示例：

```text
第1天：空仓观察
第2天：小仓试错
第3天：继续确认
第4天：冲高是否保护利润
第5天：分歧是否减仓
第10天：继续持有或退出
```

验证点：

- 决策是否前后一致；
- 是否临时乱变；
- 是否保护利润；
- 是否在逻辑破坏时退出；
- 账户总仓位是否随环境变化。

## 九、每题统一结构

详见 `TEST_CASE_TEMPLATE_2025.md`。所有测试题必须登记到 `test_case_registry`，并保存阶段A快照和哈希。

## 十、结果解释原则

- 空仓避免亏损也应得分；
- 等待承接后继续下跌，应计为等待建议有效；
- 高潮后下跌，应计为风险判断有效；
- 不能只用涨跌判断对错；
- 不得用阶段B结果倒推阶段A结论。
