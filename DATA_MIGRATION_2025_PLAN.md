# 2025 数据搬运方案

本方案只用于后续准备 2025 年多题封闭测试。本轮不实际拉取数据、不改模型规则、不调评分权重、不接真实账户、不做自动交易。

## 一、目标

为 2025 年历史模拟测试建立可复现、可审查、可防未来函数的数据底座。后续所有测试必须能回答：

- 阶段A在 `as_of_date` 当天到底能看到哪些数据；
- 阶段B只用于事后验证，不能反向影响阶段A；
- 数据是否完整、是否缺失、是否可追溯；
- 概念板块成分是否具备历史版本，如果没有，必须标记为人工观察池。

## 二、数据库设计

不覆盖现有 `data/sqlite/market_120d.sqlite`。建议新建：

```text
data/sqlite/market_2025.sqlite
```

建议新增元数据表：

| 表名 | 用途 |
| -- | -- |
| `data_manifest` | 记录数据源、时间范围、拉取时间、版本、哈希 |
| `table_coverage` | 记录每张表每个交易日覆盖率和缺口 |
| `fetch_log` | 记录每次拉取参数、返回行数、异常 |
| `snapshot_manifest` | 记录每道测试阶段A快照路径和 SHA-256 |
| `test_case_registry` | 登记每道测试题、as_of、阶段A/B窗口和状态 |

## 三、必须搬运的数据表

| 表 | 用途 | 是否必须 |
| -- | -- | -- |
| `trade_cal` | 交易日历、阶段A/B日期边界、停复牌日期判断 | 必须 |
| `stock_basic` | 股票代码、上市状态、市场、名称映射 | 必须 |
| `daily` | 个股开高低收、涨跌幅、成交量额、阶段B收益验证 | 必须 |
| `daily_basic` | 换手率、市值、量比、估值、成交活跃度 | 必须 |
| `adj_factor` | 复权收益、持仓路径收益、除权影响处理 | 必须 |
| `index_daily` | 大盘天气、指数相对收益、市场环境判断 | 必须 |
| `limit_list` | 涨跌停识别、情绪强度、不可买卖状态判断 | 必须 |
| `suspend` | 停牌识别、无法交易处理、样本有效性判断 | 必须 |

## 四、建议搬运的数据表

| 表 | 用途 | 优先级 |
| -- | -- | -- |
| `moneyflow` | 个股资金代理指标，辅助识别承接和分歧 | P2 |
| `margin` | 融资融券余额，观察趋势资金变化 | P3 |
| `top_list` | 龙虎榜席位、短线情绪、异动验证 | P3 |
| `top_inst` | 机构龙虎榜，辅助判断资金性质 | P3 |
| `index_weight` | 指数成分权重，增强指数/行业比较 | P3 |
| `concept` | 概念列表，不能单独当历史成分池 | P3 |
| `concept_detail` | 概念成分，若无历史版本必须标注风险 | P3 |
| `ths_index` | 同花顺主题指数，若权限允许可辅助概念周期 | P3 |
| `ths_member` | 同花顺主题成分，需核查是否有历史版本 | P3 |

第一版 2025 测试不应被这些增强表阻塞。必须表齐备后即可先做行业、稳定观察池和人工样本测试。

## 五、难点数据

以下数据后置，不阻塞第一版 2025 测试，但必须在报告中标明限制：

- 历史概念板块成分池；
- 题材热度；
- 新闻公告；
- 政策事件；
- 盘口承接；
- 分钟级数据。

特别注意：不能用当前概念股名单倒推 2025 年历史概念池。没有历史成分版本时，只能做“人工指定观察池测试”，不能声称是完整概念板块测试。

## 六、搬运流程设计

后续真正执行时按以下顺序：

1. 建立 `market_2025.sqlite`。
2. 拉取 `trade_cal` 并确定 2025 交易日。
3. 拉取 `stock_basic`，锁定 2025 可交易股票范围。
4. 按交易日批量拉取 `daily`、`daily_basic`、`adj_factor`。
5. 拉取主要指数 `index_daily`，至少覆盖上证、深证、创业板、科创、沪深300、中证500、中证1000。
6. 拉取 `limit_list` 和 `suspend`。
7. 写入 `data_manifest`、`fetch_log`。
8. 运行完整性检查。
9. 生成 `reports/data/data_integrity_2025.md`。
10. 完整性通过后再构建测试题。

## 七、完整性检查脚本设计

后续建议新增：

```text
scripts/check_2025_data_integrity.py
```

输入：

- `data/sqlite/market_2025.sqlite`
- 目标日期范围，例如 `20250101` 到 `20251231`

输出：

- `reports/data/data_integrity_2025.md`
- `reports/data/data_integrity_2025.json`

检查内容：

1. 2025 交易日是否完整；
2. 每个交易日 `daily` 行数是否合理；
3. `daily_basic` 是否与 `daily` 匹配；
4. 指数数据是否完整；
5. 复权因子是否完整；
6. 停牌数据是否能识别；
7. 涨跌停数据是否能识别；
8. 是否存在重复行；
9. 是否存在明显缺失；
10. 是否支持 `trade_date <= as_of_date` 的只读过滤；
11. 是否能输出覆盖率表。

## 八、后续脚本清单

| 脚本 | 输入 | 输出 | 联网 | 写库 | 需人工确认 | 风险点 |
| -- | -- | -- | -- | -- | -- | -- |
| `scripts/fetch_2025_data.py` | Tushare token、日期范围 | `market_2025.sqlite` | 是 | 是 | 是 | 权限、限频、缺表 |
| `scripts/check_2025_data_integrity.py` | `market_2025.sqlite` | 完整性报告 | 否 | 否 | 否 | 误把缺失当正常 |
| `scripts/build_2025_test_cases.py` | 数据库、测试设计 | 测试题注册表 | 否 | 可选 | 是 | 样本选择偏差 |
| `scripts/run_2025_closed_tests.py` | 测试题、阶段A数据 | 阶段A答案和快照 | 否 | 是 | 否 | 未来函数 |
| `scripts/run_2025_path_simulation.py` | 连续路径题 | 路径模拟报告 | 否 | 是 | 否 | 状态继承错误 |
| `scripts/score_2025_tests.py` | 阶段A/B结果 | 分数报告 | 否 | 是 | 是 | 用结果反推评分 |
| `scripts/generate_2025_test_report.py` | 全部测试结果 | 总报告 | 否 | 否 | 否 | 结论夸大 |

## 九、进入搬运前需要确认

- Tushare token 权限是否覆盖必须表；
- 是否接受先不做完整历史概念池；
- 2025 测试先采用申万行业、稳定板块映射和人工观察池；
- 数据库放置路径是否固定为 `data/sqlite/market_2025.sqlite`；
- 是否需要保留每次拉取原始 CSV 缓存；
- 是否限制单次拉取速度，避免触发限频。
