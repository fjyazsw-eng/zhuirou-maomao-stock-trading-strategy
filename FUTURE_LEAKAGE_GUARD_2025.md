# 2025 测试未来函数防护方案

本文件规定 2025 数据搬运和封闭测试必须遵守的防未来函数规则。

## 一、核心原则

阶段A只能读取：

```text
trade_date <= as_of_date
```

阶段B只能用于事后验证，不能影响阶段A判断、标签、仓位、持仓建议或评分权重。

## 二、阶段A流程

1. 读取 `market_2025.sqlite`。
2. 过滤所有行情、基础数据、指数数据、涨跌停、停牌数据：

```sql
WHERE trade_date <= :as_of_date
```

3. 生成阶段A输入快照。
4. 生成阶段A答案。
5. 保存阶段A报告。
6. 计算 SHA-256。
7. 写入 `snapshot_manifest`。

阶段A完成前，不允许读取阶段B窗口数据。

## 三、阶段B流程

阶段B只能在阶段A快照保存并校验哈希后运行。

阶段B读取：

```text
stage_b_start <= trade_date <= stage_b_end
```

用途仅限：

- 验证收益；
- 计算最大回撤；
- 判断建议是否帮助规避风险；
- 判断是否错过结构性机会；
- 写错误归因。

## 四、禁止事项

- 禁止阶段B结果反改阶段A。
- 禁止为了让测试好看调整评分权重。
- 禁止用当前概念股名单倒推历史概念池。
- 禁止用未来新闻、公告、政策、复盘解释阶段A。
- 禁止用事后涨幅好的股票构造观察池。
- 禁止把短窗口结果说成长期胜率。

## 五、历史概念池规则

如果没有历史概念成分版本：

- 不能声称做了完整概念板块测试；
- 只能标记为“人工指定观察池测试”；
- 必须记录人工样本来源、创建时间、创建人和理由；
- 不能因为阶段B表现好再加入样本。

## 六、快照和哈希

每题阶段A必须保存：

- 输入数据范围；
- 观察池；
- 用户状态；
- 模型输出；
- 执行标签；
- 仓位建议；
- 持仓建议；
- 账户风险；
- SHA-256 哈希。

建议路径：

```text
reports/2025_tests/snapshots/{test_id}_stage_a.json
reports/2025_tests/snapshots/{test_id}_stage_a.sha256
```

## 七、数据不足处理

如果必要数据缺失，输出：

```text
数据不足，测试无效
```

不能硬做、不能编造、不能换题、不能用阶段B倒推。

## 八、审查清单

每道题运行前必须确认：

- `as_of_date` 已明确；
- 阶段A窗口已明确；
- 阶段B窗口已明确；
- 数据库版本已记录；
- 数据覆盖报告已通过或已标记缺口；
- 概念池来源已说明；
- 用户状态已明确；
- 阶段A快照路径和哈希已生成。
