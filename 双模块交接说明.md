# 股票AI交易助手双模块交接说明

## 项目定位

股票AI交易助手是本地股票分析、持仓咨询和实时盯盘项目。

长期边界：

- 不接真实交易账户。
- 不自动交易。
- 不真实下单。
- 不编造行情和价格。
- 用户实际持仓只做盯盘和咨询。

## 两大用户模块

### 1. 股票分析工程

首要入口：`股票分析工程文件.md`。

主要能力：

- 大盘环境判断。
- 板块周期与强弱判断。
- 龙头、核心、补涨和后排识别。
- 空仓候选与买点质量判断。
- 持仓、减仓风险、止损止盈和仓位咨询。
- Tushare 历史行情、基础数据和日线资金数据。

### 2. 实时盯盘模块

首要入口：`REALTIME_WATCH_MONITOR.md`。

主要能力：

- 东方财富一分钟分时和盘中资金流。
- 5分钟承接与站稳判断。
- 大盘、板块和参照核心股联动。
- 候选股观察区提醒。
- 实际持仓保护位、风险线、修复位和转强位提醒。
- 通过 CC Connect 向微信、飞书发送状态变化。
- 后台运行、崩溃拉起、开机启动和配置热加载。

## 新环境阅读顺序

1. `AGENTS.md`
2. `PROJECT_CONTEXT.md`
3. `股票分析工程文件.md`
4. `REALTIME_WATCH_MONITOR.md`
5. `config/realtime_watchlist.json`
6. `PROJECT_STATUS.md`
7. `DAILY_WORKFLOW.md`
8. `REPORT_READING_GUIDE.md`

## 运行入口

分析与日报入口参考 `PROJECT_CONTEXT.md` 和 `DAILY_WORKFLOW.md`。

实时盯盘单次诊断：

```powershell
.\.venv\Scripts\python.exe scripts\run_realtime_watch_monitor.py --once
```

持续盯盘与消息发送：

```powershell
.\.venv\Scripts\python.exe scripts\run_realtime_watch_monitor.py --send
```

## 备份不包含

- `.env` 及任何真实 Token、API Key、Webhook、App Secret。
- `~/.cc-connect/config.toml` 和微信、飞书账号凭据。
- `.venv` Python虚拟环境。
- 行情数据库、长历史缓存和运行报告。
- Codex本地对话历史。

## 恢复运行前必须补充

1. 按 `requirements.txt` 创建Python环境。
2. 在目标机器配置 Tushare Token。
3. 若需要微信、飞书交互，单独安装并配置 CC Connect。
4. 检查 `config/realtime_watchlist.json` 中持仓、成本和盯盘条件是否仍为最新版。
5. 先运行测试和单次诊断，再启动后台服务。

## 最低验收

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py"
.\.venv\Scripts\python.exe scripts\run_realtime_watch_monitor.py --once
```

只有数据日期、接口状态、测试和消息通道都正常后，才启用持续盯盘。
